spring:
  batch:
    initialize-schema: always
datasource:
  batch-db:
    jdbc-url: jdbc:postgresql://localhost:5432/batchdb
    username: user
    password: password
    driverClassName: org.postgresql.Driver
  customer-db:
    jdbc-url: jdbc:postgresql://localhost:5432/customer
    username: user
    password: password
    driverClassName: org.postgresql.Driver
